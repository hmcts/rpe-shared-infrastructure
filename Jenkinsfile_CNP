#!groovy

//noinspection GroovyUnusedAssignment
@Library("Infrastructure@pipeline-warning-keyvault") _

def product = 'rpe'

withInfraPipeline(product) {
  enableSlackNotifications('rpe-build-notices')
}
